name: 01-System Update 

on:
  workflow_dispatch: #manually action run krna

permissions:
  id-token: write
  contents: read

jobs:
  system_update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

